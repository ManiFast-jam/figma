/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/figma',
  assetPrefix: '/figma/',
}

module.exports = nextConfig

